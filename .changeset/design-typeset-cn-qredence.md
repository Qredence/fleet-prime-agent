---
"@qredence/fleet": patch
---

Add shadcn Typeset for chat markdown, migrate `cn` to `cn@0.3.0` (`createCn` with Fleet font-size groups), and reorganize `qredence-ui` into coherent chat/panels folders without changing public behavior.
