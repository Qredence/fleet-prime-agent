#!/usr/bin/env bash
# fleet-prime — launches the web frontend + backend bridge (not the TUI).
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

cd "$SCRIPT_DIR/apps/web"
npm run dev
