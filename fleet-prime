#!/usr/bin/env bash
# fleet-prime — same as ./fleet-prime.sh: web dev server, --host/--port/--help, install.
set -euo pipefail

SCRIPT_DIR="$(cd -P "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"

exec "$SCRIPT_DIR/fleet-prime.sh" "$@"
